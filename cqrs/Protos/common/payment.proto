syntax = "proto3";

enum PaymentStatus {
  ACTIVE = 0;
  CANCELLED = 1;
}

message Payment {
  string id = 1;
  float amount = 2;
  int64 startTimestamp = 3;
  int64 lastRepayTimestamp = 4;
  PaymentStatus status = 5;
  int64 interval = 6;
  string accountId = 7;
  string recipient = 8;
}